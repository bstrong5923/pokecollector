import k from "../kaplayCtx";
import { screenWidth, screenHeight } from "../kaplayCtx";

export default function mainMenu() {
    const wheelWidth = 600;
    const wheel = [
        k.add([k.sprite("frame-1"), k.pos(screenWidth / 2, screenHeight / 2)]),
    ];
}